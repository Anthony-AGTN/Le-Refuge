{% extends 'base.html.twig' %}

{% block title %}Care{% endblock %}

{% block body %}
    <h1>Care</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ care.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ care.date ? care.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ care.description }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ care.result }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_care_index') }}">back to list</a>

    <a href="{{ path('app_care_edit', {'id': care.id}) }}">edit</a>

    {{ include('care/_delete_form.html.twig') }}
{% endblock %}
