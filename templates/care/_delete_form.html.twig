<form method="post" action="{{ path('app_care_delete', {'id': care.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ care.id) }}">
    <button class="btn">Delete</button>
</form>
