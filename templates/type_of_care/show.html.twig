{% extends 'base.html.twig' %}

{% block title %}TypeOfCare{% endblock %}

{% block body %}
    <h1>TypeOfCare</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_care.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_care.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_care_index') }}">back to list</a>

    <a href="{{ path('app_type_of_care_edit', {'id': type_of_care.id}) }}">edit</a>

    {{ include('type_of_care/_delete_form.html.twig') }}
{% endblock %}
