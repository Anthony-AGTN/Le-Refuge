<form method="post" action="{{ path('app_animal_keeper_delete', {'id': animal_keeper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal_keeper.id) }}">
    <button class="btn">Delete</button>
</form>
